<% if @user.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      表格填充有 <%= pluralize(@user.errors.count, "")%> 个错误
    </div>
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>